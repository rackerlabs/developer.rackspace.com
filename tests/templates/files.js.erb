#!/usr/bin/env node

<%= inject :auth_client %>

<% inject :create_container do |todo| %>
  <% case todo
     when 'handle as appropriate' %>
    throw new Error(err);
  <% else %>
    // <%= todo %>
  <% end %>
<% end %>
